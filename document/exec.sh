#!/bin/bash

# db
# psql -c "CREATE DATABASE tasks"
# psql tasks < "ddl/ddl_v00.sql"

# install
# sudo bundle install

# test
# sudo bundle exec rspec spec/lib/biz_spec.rb

# exec
sudo bundle exec ruby app.rb
